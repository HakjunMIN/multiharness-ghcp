# Feature Spec

## Problem Statement

사용자 관점의 문제를 기록합니다.

## Solution

사용자 관점의 해결을 기록합니다.

## User Stories

번호가 있는 actor, feature, benefit 형식으로 기록합니다.

## Implementation Decisions

`POST /api/consult` envelope, Microsoft Agent Framework 경계, APIM/Foundry IQ
adapter, citation contract, no-evidence behavior와 React UI states 결정을
기록합니다.

## Testing Decisions

network-free unit/integration tests, React component tests, recorded response,
gated Python `e2e` APIM smoke, deterministic browser e2e와 gated live browser
e2e에서 관찰할 동작을 기록합니다.

## Out of Scope

이번 spec이 만들지 않는 것을 기록합니다.
