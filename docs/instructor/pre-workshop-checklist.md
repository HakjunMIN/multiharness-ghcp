# 사전 점검

- [ ] GHCP `/agent`에 Claude agent가 보인다.
- [ ] `/model`에 Claude Opus 5, GPT-5.6 Sol, Claude Sonnet 5가 보인다.
- [ ] `npx skills@latest add mattpocock/skills`에 접근할 수 있다.
- [ ] 강사 리포가 fork 가능(public 또는 참가자 전원을 outside collaborator로 추가한 private)하고, fork에서 Issues가 기본 활성화된다. 참가자에게 강사 리포 자체의 write 권한은 주지 않는다(Issue/브랜치 번호 충돌 방지). private 리포는 최소 read 권한의 collaborator만 fork할 수 있으므로, 참가자 GitHub id를 미리 받아 초대한다.
- [ ] `azure-setup.md`의 D-7 프로비저닝과 D-1 capacity 점검을 완료했다.
- [ ] 참가자별 APIM subscription key로 모델과 retrieve smoke가 통과한다.
- [ ] `BRAND_NAME`과 `BRAND_DOMAINS`를 포함한 개인별 handout을 준비했다.
- [ ] API 기본 테스트와 React 테스트/build가 통과한다.
- [ ] participant bundle에는 instructor checkpoint와 `.env`가 없다.
- [ ] 별도 checkpoint archive 생성과 보안 전달 경로를 강사끼리 확인했다.
